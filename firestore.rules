rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections for development purposes.
    // WARNING: This is insecure for production. You should implement
    // proper authentication and security rules before deploying to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
